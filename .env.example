# Arquivo de Exemplo de Variáveis de Ambiente para o Projeto gopilot
# Copie este arquivo para .env e preencha com os valores desejados.

# --------------------------------------------------------------------------
# Configuração Geral
# --------------------------------------------------------------------------

# Define o nível de log para os serviços (e.g., DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# --------------------------------------------------------------------------
# URLs de Serviço (para comunicação interna na rede Docker)
# Estes valores são usados pelos serviços para se comunicarem entre si.
# Não use localhost; use os nomes dos serviços definidos no docker-compose.yml.
# --------------------------------------------------------------------------

ORCHESTRATOR_URL=http://orchestrator:8000
GENAI_TOOLBOX_URL=http://genai-toolbox:8080
COGNEE_SERVICE_URL=http://cognee-service:8000

# --------------------------------------------------------------------------
# Configurações do LibreChat
# Estas são algumas das variáveis que o LibreChat pode precisar.
# Consulte o arquivo .env.example dentro do diretório LibreChat para a lista completa.
# --------------------------------------------------------------------------

# Exemplo de como o LibreChat se conectaria a um endpoint de IA (neste caso, nosso orquestrador)
# (A configuração exata pode variar)
OPENAI_API_BASE_URL=${ORCHESTRATOR_URL}/v1

# Chaves de segurança para o LibreChat (gere valores aleatórios e seguros para produção)
JWT_SECRET="" # IMPORTANTE: Gere um segredo seguro para esta variável. Ex: openssl rand -hex 32
JWT_REFRESH_SECRET="" # IMPORTANTE: Gere um segredo seguro para esta variável. Ex: openssl rand -hex 32

# --------------------------------------------------------------------------
# Outras Chaves de API (se necessário)
# --------------------------------------------------------------------------

# Exemplo: Chave para um serviço externo de IA
# EXTERNAL_API_KEY=your_api_key_here
